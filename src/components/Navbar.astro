---
const rightLinks = [
  { name: 'HOME', href: '/' },
  { name: 'MODES', href: '/modes' },
  { name: 'INSIDES', href: '/blog' },
];

const currentPath = new URL(Astro.request.url).pathname;
const cartItems = 3;
---

<header class="fixed top-4 left-1/2 -translate-x-1/2 w-[95%] max-w-7xl z-50 bg-white/[0.02] backdrop-blur-2xl border border-white/10 h-20 rounded-[2rem] shadow-[0_20px_50px_rgba(0,0,0,0.5)]">
  <div class="w-full px-8 flex justify-between items-center h-full">

    <div class="flex items-center gap-4">
      <button
          id="copyIP"
          class="flex items-center gap-3 px-5 py-2.5 bg-cyan-500/10 border border-cyan-400/20 rounded-2xl text-cyan-400 font-black text-xs tracking-widest hover:bg-cyan-500 hover:text-black transition-all duration-500 group shadow-[0_0_20px_rgba(34,211,238,0.1)]"
      >
        <div class="w-2 h-2 bg-cyan-400 rounded-full animate-pulse group-hover:bg-black"></div>
        <span id="ipText">PLAY.TURBOBLOCKS.EU</span>
      </button>

      <a
          href="https://discord.gg/turboblocks"
          target="_blank"
          class="hidden sm:flex items-center gap-2 px-5 py-2.5 bg-indigo-500/10 border border-indigo-500/20 rounded-2xl text-indigo-400 font-black text-xs tracking-widest hover:bg-indigo-500 hover:text-white transition-all duration-300"
      >
        DISCORD
      </a>
    </div>

    <nav class="hidden lg:flex items-center gap-2 ml-auto">
      {rightLinks.map(link => {
        const active = currentPath === link.href;
        return (
            <a
                href={link.href}
                class={`relative px-6 py-2 text-[10px] font-[1000] italic uppercase tracking-[0.2em] transition-all duration-500 rounded-full border ${
                    active
                        ? 'text-white border-cyan-400/50 bg-cyan-400/5 shadow-[0_0_15px_rgba(34,211,238,0.2)]'
                        : 'text-white/40 border-transparent hover:text-white hover:border-white/10 hover:bg-white/5'
                }`}
            >
              <span class="relative z-10">{link.name}</span>
            </a>
        );
      })}

      <a
          href="/shop"
          class="relative flex items-center gap-3 px-6 py-3 rounded-2xl bg-gradient-to-r from-pink-500 to-purple-600 text-white text-xs font-black tracking-widest hover:scale-105 transition-all duration-500 shadow-[0_10px_20px_rgba(236,72,153,0.3)] group ml-4"
      >
        <svg class="w-4 h-4 group-hover:rotate-12 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
        </svg>
        STORE
      </a>

      <button
          onclick="toggleCart()"
          class="relative flex items-center justify-center w-10 h-10 rounded-2xl bg-gradient-to-r from-pink-500 to-purple-600 text-white hover:scale-105 transition-all duration-500 shadow-[0_10px_20px_rgba(236,72,153,0.3)] ml-2 group"
      >
        <div class="relative">
          🛒
          <span id="navbar-cart-count" class="absolute -top-2 -right-2 w-4 h-4 bg-white text-pink-600 text-[10px] font-black rounded-full flex items-center justify-center opacity-0 transition-opacity shadow-sm">0</span>
        </div>
      </button>
    </nav>

    <button id="menuBtn" class="lg:hidden p-3 bg-white/5 rounded-xl text-white">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
      </svg>
    </button>
  </div>

  <div id="mobileMenu" class="hidden absolute top-24 left-0 right-0 bg-[#0a0a0a]/95 backdrop-blur-3xl border border-white/10 rounded-[2rem] p-8 mx-4 shadow-2xl">
    <div class="flex flex-col gap-6 text-center">
      {rightLinks.map(link => (
          <a href={link.href} class="text-xl font-black italic text-white/60 hover:text-cyan-400 transition-colors uppercase">
            {link.name}
          </a>
      ))}
      <a href="/shop" class="py-4 bg-gradient-to-r from-pink-500 to-purple-600 rounded-2xl text-white font-black uppercase">
        STORE ({cartItems})
      </a>
    </div>
  </div>
</header>

<script>
  // IP Copy Logic
  const copyBtn = document.getElementById('copyIP');
  const ipText = document.getElementById('ipText');

  copyBtn?.addEventListener('click', () => {
    const ip = "play.turboblocks.eu";
    navigator.clipboard.writeText(ip);
    if (ipText) {
      ipText.innerText = "COPIED!";
      copyBtn.style.backgroundColor = "rgba(34, 211, 238, 0.4)";
      setTimeout(() => {
        ipText.innerText = ip.toUpperCase();
        copyBtn.style.backgroundColor = "";
      }, 1500);
    }
  });

  // Mobile Menu Logic
  const menuBtn = document.getElementById('menuBtn');
  const mobileMenu = document.getElementById('mobileMenu');

  menuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
</script>